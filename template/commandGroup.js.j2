#!/usr/bin/env node
let Program = require('commander')
let Actions = require('./{{ name }}')

for (let action in Actions){
  Actions[action](Program)
}

Program
.parse(process.argv)

if (Program.args.length === 0) {
  Program.help()
}
